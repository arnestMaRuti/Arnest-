if ($status->payment_status_description == "Completed") {

    $user_id = $_SESSION['user_id'];

    // unlock chat
    $conn->query("UPDATE users SET chat_access=1 WHERE id=$user_id");

    // save payment
    $conn->query("INSERT INTO payments (user_id, order_id, tracking_id, amount, status, plan)
    VALUES ($user_id, 'ORDER', '$orderTrackingId', 500, 'Completed', 'chat_unlock')");

    echo "🎉 Chat unlocked! You can now access AI chat.";
}
