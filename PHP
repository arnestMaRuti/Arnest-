<?php
include "config.php";

$user_id = $_SESSION['user_id'];

$user = $conn->query("SELECT * FROM users WHERE id=$user_id")->fetch_assoc();

if ($user['chat_access'] == 0) {
    echo "<h2>🔒 Chat is locked</h2>";
    echo "<p>You must pay to unlock AI chat.</p>";
    echo '<a href="upgrade.php">💳 Pay to Unlock Chat</a>';
    exit;
}
?>
